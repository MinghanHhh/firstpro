spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/firstpro?userUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
        username: root
        password: root

mybatis:
    mapper-location: classpath:mapper/*.xml
    type-aliases-package: com.lmh.firstpro.pojo

server:
    port: 8082
